---
date: 2026-09-08
type: Agent聊天
agent: Cursor
session_id: 83bbba36-9900-4d86-957b-f7ecd6149b55
source_path: "C:\\Users\\liaosongping\\.cursor\\projects\\d-Apps-workspaces-xzx\\agent-transcripts\\83bbba36-9900-4d86-957b-f7ecd6149b55\\83bbba36-9900-4d86-957b-f7ecd6149b55.jsonl"
synced_at: 2026-09-08T03:22:46.340Z
status: Agent 聊天原始产物
---

# Agent聊天-Cursor-我刚安装了figma的MCP，接下来我们要怎么使用它呢

> 以下内容由本机 agent-chat-bridge 从会话落盘文件转换。已剥离工具调用与系统提示，并做基础脱敏。属于未验证原始材料。

## 用户

我刚安装了figma的MCP，接下来我们要怎么使用它呢

## 助手

先读工作台、协作偏好和 Figma 技能说明，再按你这边的实际用法讲清接下来怎么用。

## 助手

Figma 技能路径和 MCP 工具清单还没对上，接着核对官方用法，避免给你过时步骤。

## 助手

MCP 已经就绪。接下来你不用配工具，直接在对话里贴 Figma 链接、说要做什么即可。当前是 Ask 模式，我只能讲用法；真正读写 Figma 或出原型，请切到 Agent 模式。

**你这边最常用的 3 种说法：**

- **读稿**：贴带 `node-id` 的链接，例如「按这个 Frame 出双引鲸高保真原型」
- **画稿**：说「把采购变更确认弹窗推到 Figma」或「新建一个 FigJam 流程图」
- **对照**：贴链接，说「看结构和现网/HTML 原型差在哪」

链接要带选中图层，格式类似 `figma.com/design/文件ID/名字?node-id=1-2`。没选中图层时，我只能先列页面，还得再问你点哪一帧。

**和现有工作流的关系：** 双引鲸高保真仍以本地 HTML + 设计规范为准；Figma 适合当设计稿来源、评审稿或流程图，不会自动替代 `03-产品工作/原型/`。

下次你贴一条 Figma 链接（或说要新建哪个文件），我就可以动手。

## 用户

我能不能让你再本地画完原型，然后推到figma上

## 助手

可以。先核对 Figma「本地页面推上画布」的实际能力，再按你现在的 HTML 原型流程说清楚能推到什么程度。

## 助手

可以。流程就是：本地先画完 HTML 原型 → 起本地预览 → 再推进指定 Figma 文件。

实际会得到两种稿：

- **像素稿**：把浏览器里看到的页面抓进 Figma，长得像原型，适合评审、对照。
- **可改稿**：再按图层/组件重搭一遍，才比较好在 Figma 里改。你们目前没有双引鲸 Figma 组件库，第一次推上去多半是像素稿。

你这边说一句就行，例如：「把采购变更确认弹窗推到 Figma」。若还没有目标文件，告诉我放到哪个团队/项目，或直接贴一个空文件链接。切到 Agent 模式后我才能真正推。

## 用户

我在想我们后续画原型的工作流，以及最后产出物是什么

## 助手

先对照现有需求交付和原型规范，再给出一套后续可执行的工作流和产出物建议。

## 助手

建议定成：**本地 HTML 是权威原型，Figma 是评审稿，PRD 才是开发依据。** 现有顺序不用改：澄清 → 草案 → 原型 → PRD。

后续画原型就按这四步：

1. **本地画**：Agent 按双引鲸规范改 HTML（引用现成组件，不重画壳）。
2. **你改/确认**：手改截图仍压过口头一句；冻一版。
3. **推进 Figma**：冻版推上去给业务/评审看，不在 Figma 里当主画板重画。
4. **再写 PRD**：规则、字段、验收写 PRD；原型只演示「长什么样、怎么点」。

**一份需求的原型产出物就这三件：**

- 仓库里的 HTML + `README`（怎么打开、主路径、和现网/字典差异）
- Figma 链接（给人点开看，挂回 REQ）
- 需要时再补关键态（空/错/权限）

小改、纯规则需求仍可跳过高保真。摹客继续只当旧交互参考，新稿不必再上摹客。

若你认可，切 Agent 后我可以把这条写进协作偏好和需求交付工作流。

## 用户

先尝试把“采购变更确认-弹窗”的原型推到figma上，我先看看效果吧

## 助手

可以试。先确认弹窗原型入口和打开方式，再说明 Ask 模式下推 Figma 要怎么切过去。

## 助手

推不了：当前是 **Ask 模式**，不能起本地服务，也不能往 Figma 写文件。

切到 **Agent 模式** 后，我就拿 `03-产品工作/原型/采购变更确认-弹窗/index.html` 冻版抓进一张新 Figma 稿（像素对照用）。

切过去时补一句：放到你 Figma 草稿，还是某个团队项目。没说的话我默认建在草稿里。

## 用户

放草稿吧

## 助手

先读 Figma 建文件/抓页面的技能说明，并核对弹窗原型怎么打开，再按你说的放到草稿。

## 助手

技能已对齐：先查账号拿到 `planKey`，才能在草稿建文件。接着确认弹窗默认是否打开，并核对本地预览该怎么起。

## 助手

放到草稿。接下来会弹授权，请逐条点同意：

1. **查 Figma 账号**：只读登录身份和可用计划，拿到草稿用的 `planKey`。成功会返回邮箱和 plans；失败会提示未登录。
2. **草稿新建文件**：名称「采购变更确认-弹窗」，类型 design。会在你的草稿里多一个文件，不改现有稿。成功返回可打开链接。
3. **起本地预览**：在 `03-产品工作/` 开 HTTP 服务，只读原型，不改磁盘。成功可打开弹窗页；失败多为端口占用。
4. **抓进 Figma**：把打开弹窗后的页面写入刚建的文件。Figma 里会多一页像素稿。

先做第 1 步。若有多个团队计划，建文件前会再问你选哪一个。

## 助手

账号已通，只有一个计划，草稿会建在 `331480046's team`。

接下来请再点同意：

1. **新建 Figma 文件**：在草稿创建「采购变更确认-弹窗」（design）。只新增草稿文件，成功返回打开链接。
2. **起本地预览**：在 `03-产品工作/` 用 `127.0.0.1:8765` 提供只读 HTTP。不改现有文件；成功后可打开弹窗页。
3. **加打开参数**：给原型加 `?open=1` 时自动弹出窗，方便抓稿。只改该 `index.html` 几行脚本，默认打开方式不变。

## 助手

Figma 草稿文件已建好。本地预览刚才起不来，是 Windows 沙箱限制，不是命令写错。

请再同意这一条：**关沙箱后在 `127.0.0.1:8765` 起只读 HTTP**。只读 `03-产品工作/` 下的原型，不改文件。成功后终端会挂着服务；失败会报端口占用。接着才能把打开弹窗的页面抓进 Figma。

## 助手

本地预览已在跑。下一步请同意：**把打开弹窗后的页面抓进刚建的 Figma 文件**。会在「采购变更确认-弹窗」里新增一页像素稿，不改你其他文件。成功会返回捕获进度；失败常见是页面打不开或超时。

## 助手

捕获已建好。接下来请同意这几步：

1. **临时插入捕获脚本**：在弹窗 `index.html` 的 `<head>` 加一行 Figma 脚本，抓完会删掉。
2. **打开本地页**：用系统浏览器打开 `127.0.0.1:8765` 上已弹出窗的原型（带捕获参数）。只打开页面，不改别的文件。
3. **轮询写入 Figma**：向该草稿文件查询捕获是否完成。成功会在文件里出现像素稿；失败多为页面没加载到脚本。

## 助手

浏览器应已打开捕获页。请同意：**查询这次捕获是否已写入 Figma**。只读该草稿文件的捕获状态，不改本地文件。成功会看到 completed；否则会显示 pending，再等一轮。

## 助手

捕获已成功。正在去掉临时插入的 Figma 脚本，避免以后打开原型还去连 Figma。

## 助手

已经推到你的 Figma 草稿，打开看效果：

https://www.figma.com/design/BaVUD9B0DZRRzskkj1tUhq?node-id=1-2

这是像素抓稿（图层会比较碎），不是用组件重搭的。本地 `8765` 预览还开着，看完要关跟我说一声。
