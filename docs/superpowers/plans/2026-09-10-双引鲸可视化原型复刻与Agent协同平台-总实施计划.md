---
title: 双引鲸可视化原型复刻与Agent协同平台总实施计划
type: master_implementation_plan
status: 进行中
owner: 廖送平
branch: codex/dyj-prototype-platform
source: 2026-09-10 用户提供的 Master Implementation Plan
updated: 2026-09-11
---

# 双引鲸可视化原型复刻与 Agent 协同平台
## Master Implementation Plan

> **For Codex:** 本文是本项目唯一的总实施计划与执行进展权威。必须先完成项目勘察和架构确认，再开始编码。禁止跳过 Registry / Schema 基础设施直接制作页面。所有功能必须可测试、可验证，并保持后续 Visual Editor 和 Agent 可持续扩展。

## 执行看板

| 项目 | 当前事实 |
|---|---|
| 实施分支 | `codex/dyj-prototype-platform` |
| 当前 Phase | Phase 2 已完成，等待阶段检查与廖送平确认；Phase 3 未开始 |
| 最近项目提交 | `ec3dbd3`（完成 Phase 1 Registry 基座） |
| 已完成产物 | [架构勘察](../../双引鲸复刻原型平台/architecture.md)、[页面清单](../../双引鲸复刻原型平台/page-inventory.md)、[组件清单](../../双引鲸复刻原型平台/component-inventory.md)、[交互清单](../../双引鲸复刻原型平台/interaction-inventory.md)、[设计系统清单](../../双引鲸复刻原型平台/design-system-inventory.md) |
| 全局阻塞 | 已有 Raw 自动同步的未提交改动，不属于本项目；远程仓库可见性未验证，暂不推送内部文档 |
| 复刻策略 | 先完成“产品管理 / 产品资料”单页垂直切片试点，包含最小可用 Visual Editor；试点验收确认前，禁止扩展首页和其他业务模块 |
| 下一步唯一入口 | 完成 Phase 2 阶段门禁并获得确认；确认后才建立 Schema Renderer |

### 执行记录规则

- 本计划是唯一的阶段任务、实际进展、验证和下一步来源；不再拆分阶段实施计划。
- 每个 Phase 的“执行记录”只填写已发生事实；“执行任务”只列尚待完成、可验证的任务。
- 每项任务完成后记录实际改动、测试命令与结果、commit；中断前更新当前 Phase 的“阻塞与下一步”。
- 每个 Phase 的最后一条执行记录必须追加“阶段门禁”结果（测试、检查、确认）；未通过或未确认时，该 Phase 状态为“待确认”，不得启动下一 Phase。
- 原系统仅为 Observation / Reference；未经验证的行为只能作为 Mock 演示，不能写成已验证业务事实。

### 2026-09-11 实施策略变更：单页试点后扩展

**已确认需求**：先跑通一个模块或一个页面的复刻试点，得到廖送平确认后，再复刻后续模块。

- 默认试点页定为 **产品管理 / 产品资料**（`/tsProductInformation`）。它覆盖全局壳、菜单和页签、列表、筛选、抽屉、表单弹窗、行操作、确认框及本地 Mock CRUD，足以验证平台路线和复刻质量；不是因为它已代表完整业务模块。
- 首页 `/tsHome` 从原“阶段 1 并行交付”调整为“试点确认后的第一扩展页”。产品资料试点未确认前，首页只能保留必要的导航占位，不得制作正式复刻页。
- 基础设施仍遵守 Registry First / Schema First；为试点实现的是最小可运行闭环，不能绕开 Registry 或把产品资料写成唯一的硬编码 JSX 页面。
- 试点包含最小可用 Visual Editor：页面/组件树、组件选中、属性面板、受控 Schema Patch、校验、预览和本地保存；编辑器修改后必须立即由同一份 Schema 驱动运行页面更新。
- 试点不包含自由拖拽排版、组件新增/复制/移动/排序、撤销重做、版本发布、审计日志和 Agent 改页；这些能力仍在产品资料试点确认后的独立 Phase 实施。
- 试点中发现的字段、视觉、交互或抽象问题，先修正 Registry、Schema、组件和设计规范，并登记扩展待办；不得带着已知偏差批量复制。

**试点验收输出**：可运行页面与交互证据、Registry / Schema / Mock / 组件清单、最小编辑器演示与 Schema Patch 结果、现网差异及处理结果、自动化测试结果、待扩展项。廖送平明确“产品资料试点确认，可扩展”后，才可启动首页及其他模块。

### 阶段门禁（所有 Phase 强制执行）

每个 Phase 的任务全部完成后，按以下顺序关闭阶段，并将结果直接写入该 Phase 的“执行记录”末行：

1. **测试**：运行本 Phase 约定的单元、集成、E2E 或视觉测试；记录命令、结果和未覆盖项。
2. **检查**：复核完成标准、Schema / Registry 一致性、页面可运行性、文档和 Git 状态；必要时人工走查关键交互。
3. **确认**：向廖送平提交阶段验收结论、证据和待决项；得到明确“确认进入下一 Phase”后，将阶段标为“已确认”。

阶段记录采用以下格式；三个结论均为通过，且“确认”已取得，才可以把下一 Phase 写为“可开始”。

| 门禁项 | 结论 | 证据 / 备注 | 确认人 | 日期 |
|---|---|---|---|---|
| 测试 | 待执行 / 通过 / 不通过 | 命令与结果 | Codex | — |
| 检查 | 待执行 / 通过 / 不通过 | 完成标准与人工走查 | Codex | — |
| 确认 | 待确认 / 已确认 / 驳回 | 用户验收结论 | 廖送平 | — |

---

# 1. 项目目标

构建一个高度复刻“双引鲸”后台系统的可运行 Prototype Platform。

最终系统必须同时支持：

1. 高度还原双引鲸页面、布局、组件、交互和页面流程。
2. 使用 Mock 数据完成完整交互。
3. 非程序员通过 Visual Editor 修改页面。
4. 所有页面和组件采用 Schema 驱动。
5. 使用 Registry 管理页面、组件、动作、数据、设计规范和资源。
6. 建立可复用 Component Library。
7. 建立 Design System。
8. 支持页面版本、保存、回滚、Undo/Redo。
9. 支持 Playwright 对原系统与复刻系统进行自动化验证。
10. 后续支持 Agent 通过自然语言修改/生成页面。
11. 人工 Visual Editor 与 Agent 必须修改同一套 Schema，禁止形成两套互相独立的页面体系。

# 2. 核心架构

```text
Agent → Registry 中心（Page / Component / Action / Data / Design / Asset）
      → Page Schema → Component Tree → Visual Editor / React Renderer
      → Prototype Runtime → Playwright Tests
```

# 3. 架构原则

## 3.1 Schema First

页面不能依赖硬编码 JSX 作为唯一事实来源。Page 必须可描述 `layout`、`components`、`props`、`dataBindings`、`actions`、`metadata`，React Renderer 根据 Schema 渲染页面。

## 3.2 Registry First

系统中的页面、组件、Action、数据、设计 Token、资源都必须可注册、可查询。Registry 是 Agent 和 Visual Editor 访问系统结构的统一入口。

## 3.3 Single Source of Truth

Visual Editor、Agent、开发代码 / Registry 都操作同一份 Schema。禁止编辑器、Agent、React 页面各维护一套数据。

## 3.4 原系统和复刻系统分离

双引鲸原系统只作为 Observation / Reference。复刻系统拥有自己的页面、Schema、Component、Mock Data、Action 与 Design System，且不得直接依赖原系统运行。

# 4. 技术路线

```text
React + TypeScript + Vite
Visual Editor：Puck / Craft.js + dnd-kit
UI：按双引鲸实际视觉体系组件化，不复制截图
Mock：MSW 或等价方案
Documentation：Storybook
E2E：Playwright
Version：Git
Schema：TypeScript + JSON Schema / 等价校验方案
```

具体库版本必须在项目勘察阶段确认当前稳定版本，不允许盲目使用过时依赖。

# 5. Phase 0：项目勘察

## 目标与完成标准

在任何编码之前了解当前工作区和双引鲸原系统；检查 Git、已有代码与 `package.json`、React/Vue/UI 框架、测试、页面/组件、现网访问方式、复刻范围，并建立页面清单。

输出：`architecture.md`、`page-inventory.md`、`component-inventory.md`、`interaction-inventory.md`、`design-system-inventory.md`。

### 执行任务

- [x] 检查仓库、技术栈、现有原型、设计规范与测试基线。
- [x] 确认现网产品资料只读访问与 Playwright / Chromium 本地条件。
- [x] 输出五份勘察清单，并记录阶段 1 为首页与产品资料。

### 执行记录

| 日期 | 实际改动与结果 | 验证 | Commit |
|---|---|---|---|
| 2026-09-10 | 确认无 React/Vite 应用；已有静态 HTML、双引鲸规范及 Playwright 1.62.1/Chromium；产出五份勘察清单 | `node --version`=`v24.18.1`，`npm --version`=`11.16.0`，Playwright 可加载 | `a72f642` |
| 2026-09-10 | 阶段门禁：测试通过（环境与清单已复核）；检查通过（Phase 0 输出齐全）；确认待廖送平明确同意进入 Phase 1 | 见执行看板与五份勘察清单 | — | — |
| 2026-09-11 | 阶段门禁确认：廖送平明确“确认，进入phase 1吧” | Phase 0 的技术路线、产品资料单页试点、现网只读与 Mock 边界已获确认 | 廖送平 | 2026-09-11 |

### 阻塞与下一步

- 阻塞：无。
- 下一步唯一入口：执行 Phase 1 的 Registry 基础设施。

# 6. Phase 1：建立 Registry

这是整个项目的基础设施。

## 6.1 Page Registry

至少包含 `id`、`name`、`route`、`type`、`module`、`schemaId`、`status`、`version`、`components`、`metadata`；支持 `registerPage()`、`getPage()`、`listPages()`、`updatePage()`、`removePage()`。

## 6.2 Component Registry

至少包含 `id`、`name`、`type`、`category`、`version`、`propsSchema`、`defaultProps`、`childrenRules`、`description`、`schema`；分类为 `basic`、`common`、`business`、`layout`。

## 6.3 Action Registry

管理 `navigate`、`openModal`、`closeModal`、`openDrawer`、`query`、`create`、`update`、`delete`、`refresh`、`download`、`back`；每项含 `id`、`name`、`type`、`paramsSchema`。

## 6.4 Data Registry

管理 Mock 数据的 `id`、`name`、`schema`、`mockData`、`operations`；支持 `query`、`pagination`、`create`、`update`、`delete`、`sort`、`filter`。

## 6.5 Design Registry

管理 `color`、`font`、`fontSize`、`spacing`、`radius`、`shadow`、`componentTokens`。

## 6.6 Asset Registry

管理 `icon`、`image`、`logo`、`file`。

### 执行任务

- [x] 创建 React + TypeScript + Vite 应用和单元测试基座。
- [x] 以失败测试驱动六类 Registry 的类型、注册、查询、更新和删除接口。
- [x] 注册首页、产品资料、基础组件、常用 Action、Mock 数据、初始 Design Token 与资源种子。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| 2026-09-11 | 创建独立 React + TypeScript + Vite 运行时与 Vitest 基座；实现 `Page`、`Component`、`Action`、`Data`、`Design`、`Asset` 六类 Registry 的 CRUD 接口；注册首页、产品资料和产品资料试点种子数据 | `npm test`：2 个测试文件、5 项测试通过；`npm run build`：通过 | `ec3dbd3` |
| 2026-09-11 | 阶段门禁：测试通过（`npm test`、`npm run build`）；检查通过（Registry 契约、种子、文档和 Git 暂存检查）；确认通过（廖送平已打开页面并确认产品资料试点种子信息可见） | 运行时位于 `03-产品工作/现网页面复刻原型/` | `ec3dbd3` |

### 阻塞与下一步

- 阻塞：无。
- 下一步唯一入口：执行 Phase 2 的 Schema 契约与校验器。

# 7. Phase 2：Schema 系统

建立 `ProjectSchema`、`PageSchema`、`ComponentSchema`、`ActionSchema`、`DataSchema`、`DesignSchema`。

`PageSchema` 必须描述 `pageId`、`name`、`route`、`layout`、`components`、`dataBindings`、`actions`、`metadata`。`ComponentSchema` 必须描述 `componentId`、`registryId`、`props`、`children`、`layout`、`dataBindings`、`actions`、`visible`、`locked`。

任何 Schema 保存前必须经过 `Schema → Validation → PASS Save / FAIL Error`；错误必须说明字段、原因和修复方式。

### 执行任务

- [x] 定义六类 Schema 契约与等价 TypeScript 校验器。
- [x] 为合法 Schema、未知组件、非法属性、嵌套节点错误编写失败测试。
- [x] 将校验错误转换为字段路径、原因和修复提示。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| 2026-09-11 | 新增 Project / Page / Component / Action / Data / Design Schema 契约；为产品资料建立单一 Schema 种子；校验器递归核验 Registry 引用、允许属性与组件嵌套规则，并返回字段路径、原因和修复提示 | `npm test -- --run`：3 个测试文件、8 项测试通过；`npm run build`：通过 | 待提交 |
| 2026-09-11 | 阶段门禁：测试通过；检查待执行（Schema 契约、产品资料种子、文档和 Git 状态）；确认待廖送平明确同意进入 Phase 3 | 浏览器自动视觉检查受审批服务配置阻断，需在本地页面人工复核“Schema 校验通过”状态 | 待提交 |

### 阻塞与下一步

- 阻塞：等待 Phase 2 阶段检查与廖送平确认。
- 下一步唯一入口：确认后建立 Schema Renderer。

# 8. Phase 3：Renderer

建立 `Schema → Renderer → Registry → React Component → UI`。Renderer 必须根据 Component Registry 找到对应组件，禁止页面直接大量硬编码组件映射。

### 执行任务

- [ ] 建立递归 Component Renderer、Page Renderer 和未知注册项错误边界。
- [ ] 为首页、产品资料 Schema 的最小渲染与未知组件错误编写测试。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 1、2 完成。
- 下一步唯一入口：注册最小布局、文本、按钮与表格 Renderer。

# 9. Phase 4：Design System + Component Library

先完成 `Button`、`Input`、`Select`、`Checkbox`、`Radio`、`DatePicker`、`Tag`、`Icon`；再完成 `PageHeader`、`SearchForm`、`DataTable`、`Pagination`、`Form`、`FormModal`、`DetailDrawer`、`ConfirmDialog`、`FilterPanel`、`Upload`、`EmptyState`、`StatusTag`；最后提取双引鲸业务组件。

### 执行任务

- [ ] 将现有 tokens、shell、组件规范映射到 Design Registry。
- [ ] 逐个实现基础、通用、业务组件并注册；公共组件在新页面前优先复用。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 1、3 完成。
- 下一步唯一入口：实现 AppShell 和基础表单组件。

# 10. Phase 5：双引鲸页面复刻

每页遵循：`观察原系统 → 记录页面 → 识别组件 → 识别 Action → 建立 Schema → 使用 Registry → 实现 Mock Data → 实现页面 → Playwright 验证`。

每页记录名称、路由、截图、布局、字段、按钮、表格、弹窗、抽屉、跳转、状态、特殊交互和复刻状态。

### 执行任务

- [ ] 仅复刻产品资料 `/tsProductInformation` 作为单页垂直切片试点；不制作首页正式复刻页或其他业务模块。
- [ ] 以该页完成全局壳、状态页签、快捷查询、综合查询抽屉、新建/编辑弹窗、图搜/导入/更多操作层、行操作、删除确认及本地 Mock CRUD。
- [ ] 页面可运行后接入最小可用 Visual Editor：页面/组件树、选中态、属性面板、受控属性编辑、Schema 校验、预览与本地保存；至少支持改标题、查询项、表格列、按钮文案/显隐和已注册 Action 绑定。
- [ ] 为编辑器修改 Schema 后的运行页更新、非法修改拦截和本地保存/重新加载编写测试；编辑器不得直接维护 DOM 快照或第二份页面数据。
- [ ] 更新产品资料对应的页面、组件、交互、设计系统清单和 E2E；记录所有现网差异与尚未抽象项。
- [ ] 完成“产品资料试点门禁”后停止，等待廖送平明确确认，再启动首页和后续模块。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 1–4 与 Phase 6 的最小 Mock 能力完成。
- 下一步唯一入口：采集并复核产品资料的结构与交互证据。

### 产品资料试点门禁（扩展前强制执行）

| 门禁项 | 通过标准 |
|---|---|
| 现网对照 | 路由、菜单/页签、字段、表列、抽屉、弹窗、行操作均有当次只读观察证据；差异已修正或明确登记。 |
| 可用性 | 从本地入口能进入产品资料；查询、状态页签、综合查询、分页、新建、编辑、删除确认和指定操作层均能实际执行并改变本地界面或数据。 |
| 架构 | 页面由注册的 Page / Component / Action / Data / Design / Asset 与经校验 Schema 渲染；没有独立于 Schema 的第二套页面事实源。 |
| 最小编辑器 | 能从组件树选择产品资料节点，并通过属性面板修改约定范围内的 Schema；校验、预览、本地保存/重新加载后，运行页保持一致。 |
| 复用 | 全局壳和本页已验证的通用部件可复用；只属于产品资料的部分明确标记为特有，不提前伪抽象。 |
| 质量 | 单元/集成测试、产品资料关键路径 E2E、人工视觉走查和 Git 检查均通过，并记录未覆盖项。 |
| 人工确认 | 向廖送平演示页面、证据和差异清单；只有收到“产品资料试点确认，可扩展”或等效明确结论，才可开始首页。 |

# 11. Phase 6：Mock Runtime

第一阶段不连接真实业务后端。Mock 必须支持查询、筛选、排序、分页、新增、编辑、删除、详情、弹窗和状态变化，数据结构尽量贴近原系统。

### 执行任务

- [ ] 建立统一 Mock 数据源与查询/分页/本地 CRUD 契约。
- [ ] 为产品资料的关键词、状态筛选、空态、分页和本地编辑写测试。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 1、2。
- 下一步唯一入口：建立产品资料脱敏 Mock 数据源。

# 12. Phase 7：扩展 Visual Editor

产品资料试点已验证最小编辑器的“树 → 属性 → Schema Patch → 运行页”闭环。本 Phase 在至少两个稳定 Schema 页面基础上扩展为完整编辑模式：页面/组件与页面树、Canvas、属性面板；组件新增/复制/移动/排序/隐藏/锁定；字段与 Mock 数据绑定；跳转、Modal、Drawer、查询、保存、删除、返回 Action。

### 执行任务

- [ ] 选择并验证 Puck/Craft.js 与 dnd-kit 的完整编辑能力与既有 Schema 单一事实源的适配方式。
- [ ] 在试点编辑器基础上实现画布编辑、组件新增/复制/移动/排序、隐藏/锁定和扩展属性面板。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：至少两个稳定 Schema 页面和可注册组件库。
- 下一步唯一入口：验证候选编辑器能否直接编辑 ComponentSchema。

# 13. Phase 8：Undo / Redo

Visual Editor 必须支持 `Ctrl + Z` 与 `Ctrl + Shift + Z`，且操作基于 Schema 状态而不是 DOM 快照。

### 执行任务

- [ ] 建立 Schema Patch 历史栈、撤销、重做和边界提示。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 7 的统一 Schema Patch 流程。
- 下一步唯一入口：为单个组件属性修改写撤销/重做失败测试。

# 14. Phase 9：版本管理

保存页面产生 `V1.0`、`V1.1`、`V1.2` 等版本，支持 `save`、`autosave`、`version list`、`version detail`、`compare`、`restore`、`publish`。编辑中的 Draft 不影响 Published Version。

### 执行任务

- [ ] 定义 Draft 与 Published 的 Schema 存储、版本快照、比较与恢复契约。
- [ ] 为保存、恢复、版本隔离和失败发布写测试。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 7、8。
- 下一步唯一入口：定义页面版本快照结构。

# 15. Phase 10：Preview / Publish

状态为 `Draft → Preview → Published`；用户可以修改、预览、确认、发布。

### 执行任务

- [ ] 实现 Draft、Preview、Published 的状态转换与发布保护。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 9。
- 下一步唯一入口：实现 Draft 与 Published Renderer 的隔离预览。

# 16. Phase 11：Dependency Graph

建立页面与组件依赖关系，例如 `CustomerList → SearchForm / DataTable / Pagination / FormModal`；必须能回答公共组件修改影响哪些页面。

### 执行任务

- [ ] 根据 PageSchema 与 Component Registry 生成依赖图和影响查询。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：多个已发布页面与组件。
- 下一步唯一入口：定义 `getComponentConsumers(componentId)` 查询。

# 17. Phase 12：Audit Log

记录操作人、时间、页面、组件、修改类型、修改前后、版本。Agent 修改必须标记 `source = agent`。

### 执行任务

- [ ] 为 Schema Patch、发布、恢复和 Agent 修改写不可变审计记录。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 9、10。
- 下一步唯一入口：定义 Audit Log 事件结构。

# 18. Phase 13：Playwright 自动化验证

建立 `原系统记录操作 → 复刻系统执行相同操作 → 验证结果` 流程；验证页面、URL、按钮、字段、弹窗、抽屉、导航、查询、新增、编辑、删除，视觉验证可逐步增加 Screenshot Comparison。

### 执行任务

- [ ] 建立本地应用 Playwright 配置与关键流程 E2E。
- [ ] 复用只读现网采集证据；不以自动化写入现网。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：至少一条可运行复刻路径。
- 下一步唯一入口：产品资料页面与最小编辑器的本地 Schema Patch 冒烟测试。

# 19. Phase 14：Agent 接入

Agent 不直接修改页面源码，标准流程为：用户需求 → 查询 Registry → 定位 Page/Component → 读取 Schema → 修改 Schema → Validation → Preview → Playwright → 生成新 Version。

### 执行任务

- [ ] 建立受控的 Registry 查询、Schema Patch 提案、校验、预览、测试和版本写入链路。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 9–13。
- 下一步唯一入口：定义 Agent Patch 请求和验证结果契约。

# 20. Phase 15：Agent 生成页面

最终支持“创建一个和现有客户列表风格一致的订单列表页面”：查询 Design Registry、Component Registry 和类似 Page，生成 Page Schema，绑定 Data/Action，Preview 后测试。

### 执行任务

- [ ] 建立类似页面检索、Schema 生成、绑定校验和人工发布门禁。

### 执行记录

| 日期 | 实际改动 | 验证 | Commit |
|---|---|---|---|
| — | 尚未开始 | — | — |

### 阻塞与下一步

- 前置：Phase 14 与完整组件、设计、数据资产。
- 下一步唯一入口：定义新页面生成请求与发布前检查。

# 21. 项目目录建议

```text
src/
├── registry/  (pageRegistry / componentRegistry / actionRegistry / dataRegistry / designRegistry / assetRegistry)
├── schema/    (pageSchema / componentSchema / actionSchema / dataSchema / validation)
├── components/ (basic / common / business / layout)
├── renderer/
├── editor/    (canvas / component-panel / property-panel / page-tree / history)
├── mock/
├── pages/
├── design-system/
├── version/
└── agent/
```

实际目录必须先检查现有项目结构后再调整，不能为了套模板而重构现有代码。

# 22. 关键验收标准

- **Registry**：可查询系统页面、页面组件、组件使用页面和按钮 Action。
- **Visual Editor**：无需代码可改文字、字段、列、按钮、弹窗、跳转和组件位置。
- **Runtime**：修改后页面正常运行。
- **Version**：可保存、预览、发布、回滚。
- **Agent**：修改 Schema 后页面自动更新。
- **Validation**：非法 Schema 不允许保存。
- **E2E**：核心页面的原系统与复刻系统操作流程一致。

# 23. 开发规则

1. 不允许跳过 Registry。
2. 不允许为了快速完成页面而大量硬编码。
3. 不允许 Visual Editor 单独维护另一套页面数据。
4. 不允许 Agent 直接绕过 Schema 修改 UI。
5. 公共组件必须优先进入 Component Registry。
6. 新增业务组件前先判断是否可以复用已有组件。
7. 修改公共组件前必须检查 Dependency Graph。
8. 每完成一个阶段必须运行对应测试。
9. 不允许用截图伪造交互。
10. 不允许使用真实后端作为第一阶段依赖。
11. 所有页面必须能够独立通过 Mock Runtime 运行。
12. 所有重要结构必须有 TypeScript 类型。
13. 所有 Schema 必须可验证。
14. 所有重要修改必须可以回滚。
15. 不要为了“未来可能需要”提前实现大量复杂功能。

# 24. 开发优先级

```text
P0：Registry / Schema / Renderer / Component System
P1：产品资料单页复刻 / Mock Runtime / Design System / 最小 Visual Editor
P2：扩展页面复刻 / 完整 Visual Editor / Undo-Redo / Version / Preview-Publish
P3：Dependency Graph / Audit Log / 自动化验证
P4：Agent / Agent Page Generation
```

不要一开始就开发 Agent；先把底层系统建设好，Agent 才有可靠的操作对象。

# 25. 每个阶段的完成条件与推进门禁

每个 Phase 必须同时满足：代码完成、测试通过、页面可运行、Schema 有效、Registry 正确、文档完成。不能以“代码写完了”作为阶段完成标准。

此外，必须完成本计划“阶段门禁”规定的测试、检查和廖送平确认。未取得“确认进入下一 Phase”的明确结论，任何后续 Phase 只能保持待开始，不得实施。

# 26. 最终用户体验

正常模式下像普通后台一样使用。编辑模式下点击页面和组件，在右侧修改属性，预览、保存版本并发布。未来可直接告诉 Agent“增加一个客户等级筛选条件”，由 Agent 修改 Schema、自动验证、生成新版本，用户确认后发布。

# 27. 最终成功标准

1. 用户无需代码修改页面文字。
2. 用户无需代码增加、删除、调整表格字段。
3. 用户无需代码修改按钮行为。
4. 用户无需代码增加弹窗。
5. 用户可以保存、预览、发布、回滚。
6. Agent 可以根据自然语言修改已有页面。
7. Agent 可以根据现有组件和 Design System 创建新页面。
8. 系统可以自动验证复刻页面与双引鲸原系统的关键行为。

# 28. Codex 执行要求

开始执行前必须阅读本计划、检查当前 Repository、输出项目结构和技术栈、说明与本方案的差异、给出实施顺序；不得直接开始大规模编码，必须先建立 Registry + Schema 基础架构。每完成一个 Phase 都运行测试，最终提供完整运行方式、测试方式和变更说明。

若当前代码库与本方案冲突，优先保留已有可用能力，不允许无理由重构；若技术方案有明显问题，必须先说明问题、影响和替代方案，再实施。

# 29. 项目最终形态

```text
Agent → Natural Language → Registry
Registry（Page / Component / Action / Data / Design / Asset / Dependency / Version / Audit）
                         ↓
                      Schema
                ↙                 ↘
        Visual Editor           React Renderer
                ↘                 ↙
                 Prototype Runtime
                         ↓
            Playwright Validation → Version / Publish
```

最终目标不是复制一个双引鲸页面，而是建立一个能够不断复制、编辑、复用、验证和由 Agent 持续迭代的后台原型基础设施。
