# 产品资料现网证据索引

> 范围：仅记录人工登录后进行的只读观察。截图和 DOM 原文位于本机 `08-自动化/playwright-tools/output/`（已忽略，不入库）；不保存账号凭据、Cookie、token 或现网业务数据。

| 证据 ID | 状态 | 必填证据 | 结论 |
| --- | --- | --- | --- |
| PI-001 | 默认单个产品 | 截图、DOM 文本、默认筛选值、表列 | 已采集 |
| PI-ADV | 综合查询抽屉 | 截图、字段分组、默认值、按钮 | 部分采集（截图待归档） |
| PI-COLUMNS | 列设置抽屉 | 截图、默认显示项、底栏按钮 | 部分采集（截图待归档） |
| PI-NEW | 新建产品 | 全部锚点、字段、底栏按钮 | 部分采集（提交行为待验证） |
| PI-EDIT | 编辑产品 | 全部锚点、字段、禁用态、底栏按钮 | 部分采集（提交与编辑规则待验证） |
| PI-DELETE | 删除确认 | 标题、正文、按钮顺序与样式 | 已采集（确认执行结果待验证） |
| PI-IMAGE | 图搜层 | 标题、字段、关闭入口 | 部分采集（结果行为待验证） |
| PI-IMPORT | 导入层 | 标题、提示、按钮与非目标 | 未取证（点击未出现可观察层） |
| PI-MORE | 更多操作菜单 | 菜单项与关闭方式 | 部分采集（菜单结果待验证） |
| PI-ROW-MORE | 行更多操作菜单 | 菜单项与关闭方式 | 部分采集（菜单结果待验证） |
| PI-TABS | 非默认产品页签 | 默认筛选、工具栏与列表框架 | 部分采集（查询和分页行为待验证） |

## PI-001：默认单个产品

- 采集时间：2026-09-13（本机只读采集）
- 路由：`/tsProductInformation`
- 页面标题：`产品资料 | 贸易管理系统`
- 本机证据：`08-自动化/playwright-tools/output/dyj-product-information-default-single-product-2026-09-13T10-32-56-454Z.png` 及同名 JSON
- 页签：`草稿产品`、`单个产品`（选中）、`组合产品`、`客户专属`、`全部`。
- 默认筛选：`是否有图：全部`、`是否停产：全部`、`产品状态：上架`、关键词输入框占位 `请输入`；操作入口为 `查询`、`综合查询`、`图搜`。
- 工具栏：`+ 新建产品`、`导入产品`、`批量删除`、`打印条码`、`复制产品`、`选品车`、`更多操作`，另有图标型入口。
- 已观察到表列：序号、勾选、产品图片、产品编号、辅助编号、产品名称、英文名称、出厂货号、产品类型、出厂价、进货价、自定义价格、第三方价格、采购折扣、标贴费、起订量、产品状态、操作。
- 分页：显示总记录数、默认 `100条/页`、页码和跳转页码入口；总量来自现网，不带入本地 Mock。

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口，路由为产品资料；截图仅用于本地对照，不提交仓库。
- 壳层：左侧深色一级导航宽 210px，顶栏高 60px，业务页签条高 40px；内容背景为浅灰，列表白卡从 y=114 起铺满内容区。
- 列表结构：产品状态页签 → 查询行 → 工具行 → 主表（含序号/勾选固定区、产品图片、可横向主列、右侧操作列）→ 分页；表头与表体均为 40px 行高。
- 查询与工具：关键词框在左、三项下拉与“查询/综合查询/图搜”同行；新建、导入和批量操作位于下一行，七个圆形图标位于右侧。
- 数据边界：截图中的账号、产品编号、名称、价格、记录总数和时间均是现网信息，不得复制到本地 Mock 或证据正文。

## PI-ADV：综合查询

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 触发：点击首屏“综合查询”；可通过 `Esc` 返回列表，不提交查询。
- 标题和动作：标题为“综合查询”，底部为“重置”“确定”。
- 字段分组：基础信息、产品参数、厂商信息、其他信息。
- 已观察基础信息：产品名称、出厂货号、产品编号、产品单位、分类名称、分类编号、出厂价区间、进货价区间、产品品牌、产品认证、是否在展。
- 已观察产品参数：外箱装量区间、中文包装，以及产品规格/包装规格/外箱规格的长宽高区间（单位 `cm`）。
- 已观察厂商信息：厂商编号、厂商名称、联系人、联系手机、联系电话、QQ。
- 已观察其他信息：侵权、13条形码、是否热销、是否实样、中文备注、登记人、修改人、登记时间、修改时间、下架时间。
- 默认提示：下拉框以“请选择”或“全部”显示；在空白默认表单点击“重置”后仍保持默认态，并可通过 `Esc` 关闭返回列表。本轮未填写或提交任何条件，已填条件的清空效果和查询组合结果仍待采集。

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅点击“综合查询”打开抽屉，未输入、重置或提交。
- 层级与尺寸：右侧白色抽屉宽约 850px，从顶到底；列表区覆盖 50% 黑色遮罩。顶部高约 54px，左侧关闭图标后为标题“综合查询”。
- 内容：双列表单，依次为基础信息、产品参数、厂商信息、其他信息；字段标签右对齐，控件高 32px，规格字段由三组长宽高区间和 cm 单位构成。
- 底栏：固定在抽屉底部，右侧顺序为“重置”“确定”；本轮未点击。

## PI-COLUMNS：列设置

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 触发：首屏右侧图标工具栏的第七个图标（与常规按钮分离）；此前第六个图标实际为“打印”，不得误作列设置入口。
- 标题和动作：标题“勾选您要显示的字段”；顶部提供“关闭此对话框”“设置全部显示”“排序整理”“恢复系统默认”、关键词输入框与“查询”；底部为“关闭”“保存”。本轮已确认顶部关闭可返回列表，未触发会改动账号偏好的恢复系统默认或保存。
- 已观察默认列表：已列出产品图片、产品编号、辅助编号、产品名称、英文名称、出厂货号、产品类型、出厂价、进货价、第三方价格、采购折扣、标贴费、起订量、产品状态等，并延伸至分类、包装材质、规格、厂商、状态、备注及登记时间字段。
- 本轮未改动字段勾选、排序或保存；关闭操作未改变账号配置。字段切换规则、搜索结果与保存后的列表变化仍待采集。

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅打开抽屉，未点击恢复、排序、关闭或保存。
- 层级与尺寸：右侧白色抽屉约 710px 宽，左侧为 50% 黑色遮罩；顶部为关闭图标和标题，底部固定“关闭 / 保存”。
- 内容：工具条为“设置全部显示 / 排序整理 / 恢复系统默认 / 字段搜索 / 查询”；字段是四列蓝色卡片，不是原生复选框清单。每张卡片可见眼睛图标、字段名和拖拽柄。
- 规则边界：未点击字段卡片、恢复、排序或保存；本地仅复刻初始视觉与关闭，不补造配置变更结果。

## PI-IMAGE：图搜

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅打开浮层，未填写网址、上传、粘贴或触发检索。
- 定位与尺寸：白色浮层约 706px 宽，位于查询区下方、列表之上；无整页遮罩，投影轻微。
- 内容：首行左侧为“粘贴图片网址”、右侧是输入框与搜索图标；下方三张等宽虚线卡片，顺序为上传图片、上传文件、粘贴图片，文案分别约束 JPG/PNG、EXCEL 与 Ctrl+V。
- 规则边界：上传、粘贴和图像检索后的结果尚未采集；本地只复刻已观察的初始浮层与再次点击入口关闭行为。

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 触发：点击首屏“图搜”。
- 已观察入口：粘贴图片网址输入框；上传图片（支持 `JPG/PNG`）；上传文件（仅支持 `EXCEL`）；粘贴图片区域，提示“点击此处，然后 Ctrl+v 到这里”。
- 本轮未粘贴、上传或提交任何图片/文件。可见区域未提供文本关闭按钮；`Esc` 不关闭该层，再次点击“图搜”可关闭。图片检索结果与异常反馈待采集。

## PI-MORE：更多操作

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 菜单项：批量导出、批量导入图片、批量设置产品下架、批量更新英文品名、批量样品价格调整、批量修改产品资料、批量管理证书文件、批量管理品牌文件、批量管理视频、批量加入选品车、恢复误删数据。
- 点击页面空白处可关闭；本轮未进入任一会产生业务变更、导出或上传的菜单项。

## PI-ROW-MORE：行更多操作

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 菜单项：客户产品、加入选品车、留痕记录、货柜箱/只计算。
- 点击页面空白处可关闭；本轮未执行任一菜单项。

## PI-NEW：新建产品资料

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅打开表单，未填写、保存草稿或确定。
- 层级与尺寸：右侧白色大抽屉约 1,390px 宽，左侧保持深色遮罩；顶部为关闭图标与“新建产品资料”，底部固定“保存草稿 / 关闭 / 确定”。
- 首屏结构：左列为七个纵向分区锚点；内容区以三列字段排布，依次可见基础信息、产品参数和产品图片的上传占位。多数基础字段初始态为禁用。
- 规则边界：仅复刻已观察的首屏结构、视觉与关闭；不补造字段启用、保存草稿或确定后的业务结果。

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 标题和导航：标题“新建产品资料”；分区锚点为基础信息、产品参数、产品图片、产品属性、产品条码信息、其他信息、厂商信息。
- 底部动作：保存草稿、关闭、确定；本轮仅用“关闭”返回列表。
- 基础信息已观察字段：产品编号、出厂货号、辅助编号、产品名称、英文名称、产品分类、产品单位、出厂价、进货价、第三方价格、标贴费、采购折扣、起订量、厂商编号、厂商名称、第三语种；其中产品编号、出厂货号、产品名称、产品分类、出厂价、进货价、厂商编号带必填标记。
- 产品参数、条码、其他和厂商信息已观察到规格/重量/材积、认证/品牌/材质、三类条码、状态选项、备注、联系人等字段；多数控件在本轮初始态为禁用，具体启用条件、校验、保存草稿与确定结果待采集。

## PI-TABS：非默认产品页签

- 采集时间：2026-09-14（人工登录后的只读观察）；依次切换草稿产品、组合产品、客户专属、全部后恢复“单个产品”。
- 共同结构：均保留关键词查询、表格、行操作与分页框架；均未填写、查询、翻页或执行行操作。
- 草稿产品：首屏工具栏仅保留查询、综合查询、图搜、新建产品、更多操作和图标工具组，未见导入、批量删除、打印条码、复制产品、选品车。
- 组合产品：可见产品状态筛选和综合查询；工具栏为图标工具组，未见文字型新增和批量操作按钮。
- 客户专属、全部：在本轮观察中继续使用产品列表框架；工具栏与筛选的细节、各页签的数据规则、查询及分页结果仍待验证。

## PI-EDIT：编辑产品资料

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅打开首行编辑抽屉，未改写字段或确定。
- 与新建态关系：复用相同的右侧大抽屉、左侧遮罩、纵向七分区锚点和三列首屏字段布局；编辑态底栏仅有“关闭 / 确定”，没有“保存草稿”。
- 规则边界：现网字段值不进入本地 Mock；本地仅展示脱敏空态骨架，不补造编辑保存结果。

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 标题和导航：标题“编辑产品资料”；与新建页使用相同的七个分区锚点。
- 编辑初始态：已显示既有产品字段值，但不记录进台账；基础信息、产品参数、条码、其他信息中的大多数控件为禁用。厂商信息的联系人、联系手机、联系电话、QQ 在本轮呈可输入态。
- 底部动作：仅“关闭”“确定”，未出现“保存草稿”；本轮只点击“关闭”，未修改或提交。
- 字段启用条件、各字段可编辑范围、校验以及“确定”后的结果待验证。

## PI-DELETE：删除确认

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅打开首行删除确认框，未执行确定。
- 定位与尺寸：全屏暗色遮罩之上为约 420px 宽的居中白色确认框；标题和影响提示均居中，底部横向居中排列“取消 / 确定”两个约 100px 宽按钮。
- 视觉：标题为常规字重，确认按钮为红色，提示中的“1条”为强调色；本地确定按钮只关闭浮层，不删除 Mock 数据。

- 采集时间：2026-09-14（人工登录后的只读观察）。
- 触发：点击一行的“删除”。标题和主文案均为“您确定要删除吗？”，影响提示为“此操作将会删除 1条 产品数据”。
- 按钮顺序：取消、确定；本轮点击“取消”返回列表，未执行删除。

## PI-MORE：更多操作菜单

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅展开工具栏菜单，未点击任一业务项。
- 定位与尺寸：无全页遮罩；菜单约 200px 宽，从“更多操作 >”下方、向左略外展开，白底、细边框和轻阴影；单行高度约 30px。
- 内容：共 11 项；首项“批量导出”右侧有二级箭头，其余为批量导入图片、设置产品下架、更新英文品名、样品价格调整、修改产品资料、管理证书/品牌/视频、加入选品车和恢复误删数据。
- 本地为展示型菜单；不绑定导出、导入、下架、修改或恢复等真实业务操作。

## PI-ROW-MORE：行级更多菜单

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；只展开首行“更多”，未点击菜单项。
- 交互状态：触发行呈浅蓝选中背景；无全页遮罩；菜单在操作列“更多”下方展开、右边缘对齐，约 160px 宽、约 130px 高，白底、细边框和轻阴影。
- 内容和节奏：4 个约 26px 高的单行项，依次为“客户产品 / 加入选品车 / 留痕记录 / 货柜箱/只计算”。本地只展示菜单，不执行真实业务动作。

## PI-DRAFT：草稿产品页签

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅切换“草稿产品”页签并等待加载完成，未编辑或提交数据。
- 差异状态：草稿页签为蓝色激活；查询区只保留“是否有图 / 是否停产 / 关键词 / 查询 / 综合查询 / 图搜”，隐藏“产品状态”；工具栏只保留新建、导入、批量删除、更多操作。
- 列表与分页：现网加载完成后显示草稿记录和较短分页。本地仍使用脱敏 `MOCK-DRAFT` 数据，记录数量不视为现网事实。

## PI-COMBO：组合产品页签

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅切换“组合产品”页签并等待加载完成，未编辑、导出或提交数据。
- 查询区：页签为蓝色激活；保留“是否有图 / 是否停产 / 产品状态（默认上架）/ 关键词 / 查询 / 综合查询 / 图搜”，右侧保留六个圆形列表工具图标。
- 工具栏：该页签在查询区与表头之间不显示“新建产品、导入产品、批量删除、更多操作”等左侧业务按钮。
- 列表与分页：首屏为密集表格，现网本页共 20 条；列在“产品状态”后追加“分类编号、分类名称”。本地只以 `MOCK-COMBO-*` 脱敏演示记录还原布局，数量、编码及字段值不作为现网事实。

## PI-CUSTOMER：客户专属页签

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅切换“客户专属”页签并等待加载完成，未编辑、导出或提交数据。
- 查询与工具：页签为蓝色激活；查询区和右侧六个圆形列表工具与组合产品一致；不显示“新建产品、导入产品、批量删除、更多操作”等左侧业务按钮。
- 列表与分页：默认显示“产品状态、分类编号、分类名称”等扩展列，分页为多页。观察时现网显示 409 条记录；本地仅以 `MOCK-CUSTOMER-*` 脱敏演示记录呈现，不将该总数或字段值当作本地业务数据。

## PI-ALL：全部页签

### 2026-09-14 同视口视觉基线

- 采集条件：人工登录后的只读观察，1920 × 875 视口；仅切换“全部”页签并等待加载完成，未编辑、导出或提交数据。
- 查询与工具：页签为蓝色激活；保持“是否有图 / 是否停产 / 产品状态（默认上架）”与六个圆形列表工具；不显示左侧业务工具栏。
- 列表与分页：保留“分类编号、分类名称”扩展列；长列表分页呈现前五页、向下跳页、省略号、末页和前往页码入口。现网本次观察显示 6519 条和末页 66；本地只使用已脱敏模拟集合还原结构，不纳入现网产品资料。
- 视觉对齐迭代：正式页将关键词输入置于查询行首，六个列表工具与查询条件同排，表格紧随其后；本地已按该关系重排，未改变任何查询或业务结果。
- 表头视觉复刻：已为本地非操作列加入不触发排序的浅灰升降序标记；实际排序规则仍未取证，不模拟。
- 列表视口复刻：正式“全部”页签的初始宽屏视口只展示前段业务列，操作列需横向滚动后才出现，且分页紧随固定高度的列表区；本地已按此呈现关系调整。缩略图为本地生成的通用占位图形，不使用正式系统图片。
- 查询行复核：关键词输入框右侧存在一个图标与复选框组合的匹配选项控件；本地已复刻其位置和静态外观，具体匹配语义尚未取证，不接入筛选结果。
- 分页复核：正式页含上一页和下一页入口；本地已补充同类分页导航，页数和末页均根据本地模拟数据计算，不复用正式系统记录总量。
- 分页布局复核：正式页将记录数、每页条数、页码与跳页输入组合置于列表区下方中部；本地已移除将记录数推到左侧的旧规则，按同一分组关系呈现。
- 品牌区复核：正式页左上角为白色的抽象鲸形标识配中英文名称；本地已由通用圆环替换为 CSS 绘制的抽象白色标识，不使用正式系统图片或资源文件。
- 查询按钮复核：正式页将“查询”作为蓝底主操作，“综合查询”和“图搜”为次级描边操作；本地已按该层级呈现，原有本地查询行为不变。
- 产品编号复核：正式“全部”页签的产品编号在列表中呈蓝色链接视觉；本地已在对应列复刻该色彩语义，不绑定未经取证的详情跳转行为。
- 缩略图复核：正式列表中的产品图片色调和内容各不相同；本地已以多种 CSS 抽象占位缩略图还原视觉密度，未复制正式图片或产品信息。
- 侧栏层级复核：正式页在产品资料内将“产品管理”展开，并以蓝色二级项“产品资料”标记当前位置；本地已按同一层级呈现，不再错误高亮一级菜单。
- 单个产品查询行复核：正式“单个产品”页签同样在关键词输入后展示图标与复选框组合；本地已补齐该静态控件并新增回归测试，具体匹配语义仍未取证，不接入筛选结果。

## 已知差异与待验证

- 当前本地页的总数、产品记录和图片均为脱敏演示数据；不得使用截图中的现网记录。
- 综合查询、列设置与图搜已有字段和入口证据，但截图归档及结果行为未完成；新建、编辑和删除确认已补充结构证据，提交、编辑和确认执行结果仍未验证，实施前不得补造字段或交互。
