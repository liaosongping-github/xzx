---
title: 双引鲸原型组件-视觉规范
type: prototype_visual_spec
owner: 廖送平
updated: 2026-09-05
evidence: S-001, E-005
---

# 组件视觉规范

> 尺寸、颜色、字号的唯一出处是 [`tokens.css`](../tokens.css)。HTML 片段只写 class，禁止 `style="width/color/font-size"`。

## 通用

| 项 | Token | 值 |
|---|---|---|
| 字体 | `--dyj-font-family` | 微软雅黑 |
| 正文 | `--dyj-font-size-sm` / `base` | 13px / 14px |
| 表格 | `--dyj-font-size-xs` | 12px |
| 主色 | `--dyj-color-primary` | `#009DFF` |
| 文字 | `--dyj-text-primary` | `#333` |
| 控件高 | `--dyj-control-height` | 32px |
| 表头/表体行高 | `--dyj-table-header-height` / `--dyj-table-row-height` | 40px / 40px（E-006，旧 28/32 作废） |
| 圆角 | `--dyj-radius` / `--dyj-radius-card` | 控件 4px / 白卡 6px（E-006） |
| 弹窗内容区 | `--dyj-dialog-body-padding` | 20px（与 `--dyj-space-xl` 同值） |

## 弹窗（通用）

| 项 | Token | 值 |
|---|---|---|
| 常规最小宽 | `--dyj-dialog-min-width` | 500px |
| 宽版 | `--dyj-dialog-wide-min-width` | 1400px（新建产品实测，旧 1000 作废） |
| 内容区 padding | `--dyj-dialog-body-padding` | 20px；`.dyj-modal-body` |
| 遮罩 | `--dyj-mask` | 50% 黑（E-006 实测） |

来源：原型壳层 `shell.css`。**不要**把 `--dyj-filter-gap`（15px，查询条件行间距）用到弹窗内容区。现网 EP Dialog 内边距待对照。

## 菜单（全局壳）

| 项 | Token | 值 |
|---|---|---|
| 侧栏宽 | `--dyj-sidebar-width` | 210px（收起 54px） |
| 侧栏底 | `--dyj-sidebar-bg` | `#333744` |
| 菜单字/行高 | 一级 15px/50px 高，二级 14px/40px 高（E-006） | |
| 激活 | `--dyj-sidebar-active-bg` + 白字 | `#009DFF` |
| 顶栏高 | `--dyj-header-height` | 60px（E-006，旧 50 作废） |
| 菜单查询宽 | `--dyj-header-search-width` | 220px |
| 页签条高 / 页签高 | `--dyj-tabs-height` / `--dyj-page-tab-height` | 40px / 32px |
| 激活页签 | 主色实心底 + `--dyj-text-inverse` | |

## 查询条件

| 项 | Token | 值 / 现网 |
|---|---|---|
| 行间距 | `--dyj-filter-gap` | 15px |
| 关键词宽 | `--dyj-filter-keyword-width` | 250px |
| 状态下拉宽 | `--dyj-filter-select-width` | 240px（class `is-filter`） |
| 日期框宽 | `--dyj-filter-date-width` | 120px（class `is-date`） |
| 出厂价框 | `--dyj-filter-price-width` | 72px（class `is-price`） |
| 综合查询后缀 | `--dyj-filter-append-width` | 36px |
| 查询按钮 | 主色、高 32px | |

## 列表与列设置

| 项 | Token | 值 |
|---|---|---|
| 圆形工具图标 | `--dyj-toolbar-icon-size` | 34px（E-006 实测） |
| 方形图标钮 | `--dyj-icon-btn-size` | 28px |
| 缩略图 | `--dyj-thumb-size` | 36px |
| 分页每页条数 | `--dyj-pager-select-width` | 88px（class `is-pager`） |
| 跳转页输入 | `--dyj-page-jump-width` | 48px（class `is-page-jump`） |
| 行内「详情」 | class `dyj-btn-text is-success` | 成功绿字 |
| 列设置抽屉宽 | `--dyj-drawer-col-set` | 710px（E-006 实测，旧 514 作废） |
| 综合查询抽屉宽 | `--dyj-drawer-adv-query` | 850px（E-006 实测） |
| 图搜浮层宽 | `--dyj-imagesearch-pop` | 706px（E-006 实测） |
| 字段行高 | `--dyj-table-header-height` | 40px |

## 产品选择器

| 项 | Token | 值 |
|---|---|---|
| 弹窗 | `--dyj-picker-width` × `--dyj-picker-height` | 916 × 744 |
| 遮罩 | `--dyj-mask` | 50% 黑 |
| 列表「综合查询」抽屉 | `--dyj-drawer-adv-query` | 850px（E-006 实测） |
| 分组 label 列 | `--dyj-query-label-width` | 84px |

## 禁止

- 在原型 HTML 写死 `px`、`#hex`；Element 默认蓝 `#409EFF` 仅在状态页签激活态被现网实测到（E-006，待确认是否全局，原型暂统一主色 `#009DFF`）
- 把列设置做成居中大弹窗，或把添加产品做成右侧抽屉
