# HTML 低保真原型模板

复制本目录，并命名为 `REQ-编号-需求名称/`。至少保留：

```text
REQ-编号-需求名称/
├── index.html
└── README.md
```

要求：

- 原型可以离线打开，不依赖外部 CDN。
- 页面显示关联需求编号和版本。
- README 链接到需求和 PRD；需求与 PRD 也反向链接原型。
- 明确标注低保真内容、假数据和未实现行为。

