---
description: 禁止把 Excel 等一次性产物写入知识库；用户给出的知识库路径默认是内容来源
alwaysApply: true
---

# 知识库不落一次性产物

- `02-业务知识库/`（含字段字典）、`05-决策记录/`、`06-长期记忆/` **只维护 Markdown**。
- **禁止**写入 xlsx、csv、zip、临时脚本等一次性产物。
- 用户消息里的知识库路径默认是**内容来源**，不是保存位置。
- 未明确说「保存到该路径」时，先问落盘目录，不得自行写入上述目录。
- 生成 Excel 等导出：落到用户指定路径；未指定则先问。

反例：按 `字段字典/销售报价单.md` 生成表头 Excel 时，把 xlsx 写进同一字段字典目录。
